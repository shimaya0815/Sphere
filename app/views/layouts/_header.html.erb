<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <% if user_signed_in? %>
        <a class="navbar-brand" href="/<%= current_user.office_id %>/users/<%= current_user.id %>">
          <span>Sphere</span>
        </a>
      <% else %>
        <a class="navbar-brand" href="/">
          <span>Sphere</span>
        </a>
      <% end %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">
          <% if user_signed_in? %>
            <li class="nav-item">
              <a class="nav-link text-light" href="/<%= current_user.office_id %>/users/<%= current_user.id %>">
                <i class="fa-solid fa-home"></i> マイページ
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-light" href="/<%= current_user.office_id %>/users">
                <i class="fa-solid fa-users"></i> ユーザー一覧
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-light" href="/<%= current_user.office_id %>/todos">
                <i class="fa-solid fa-list"></i> ToDoリスト
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-light" href="/<%= current_user.office_id %>/tax_clients">
                <i class="fa-solid fa-building"></i> クライアント一覧
              </a>
            </li>
            <!-- ユーザー招待リンク -->
            <% if user_signed_in? %>
              <li class="nav-item">
                <a class="nav-link text-light" href="/<%= current_user.office_id %>/users/invitation/new">
                  <i class="fas fa-user-plus"></i> ユーザー招待
                </a>
              </li>
            <% end %>
            <li class="nav-item">
              <a class="nav-link text-light" rel="nofollow" data-method="delete" href="/<%= current_user.office_id %>/users/sign_out">
                <i class="fas fa-sign-out-alt"></i> ログアウト
              </a>
            </li>
          <% else %>
            <li class="nav-item">
              <a class="nav-link text-light" href="/users/sign_up">
                <i class="fas fa-user-plus"></i> サインアップ
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-light" href="/users/sign_in">
                <i class="fas fa-sign-in-alt"></i> ログイン
              </a>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>
